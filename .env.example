# Copy to .env and fill in. .env is gitignored.
# No credentials live here — gh CLI uses the OS keychain; Claude Code auth lives in ~/.claude/ (OS keychain / encrypted).
GITHUB_USER=your-github-username
